# 1. 第一步：屏蔽当前目录及其所有子目录下的所有文件和文件夹
*

# 2. 第二步：豁免所有文件夹（非常关键！）
# 如果不加这一行，Git 会直接忽略外层文件夹，根本不会进去寻找里面的代码文件
!*/

# 3. 第三步：豁免你需要的特定后缀文件（VIP 通行证）
!*.py
!*.md
!*.sh
!*.c
!*.cpp
!*.h
!*.f90
!*.json
!*.txt

# 4. 第四步：千万别忘了豁免 .gitignore 文件本身！
!.gitignore
